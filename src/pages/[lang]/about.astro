---
import AboutPage from "../../components/AboutPage.astro";
import { nonDefaultLocales } from "../../i18n";

export function getStaticPaths() {
  return nonDefaultLocales.map((l) => ({ params: { lang: l.code } }));
}

const { lang } = Astro.params;
---

<AboutPage lang={lang!} />
